# Rust callers — `pulldown-cmark-kotlin`

> Generated by `scaffold/generate_caller_reports.py` from `workspace_dep_graph.json` (Cargo-aware dep graph). Do not hand-edit. Re-run after the graph is rebuilt.

## What this file is

For every Rust crate this repo owns (under `tmp/`), this file lists every **cross-repo** workspace caller: every `use` statement in another `*-kotlin` repo's Rust source that imports a symbol from one of this repo's crates. Symbols are grouped per-crate; codex-kotlin sites are surfaced first per symbol (codex is the priority consumer driving the port queue).

**Granularity:** import-level (each row is one `use` line). Call-site detail — where the imported name is actually invoked, implemented, pattern-matched, or used as a generic bound inside the caller body — requires a per-symbol grep across the listed caller files. The graph tells you exactly which files to grep.

**Cross-repo only.** Imports between crates inside this same repo are omitted — those are intra-repo refactoring concerns, not the cross-project porting concern this file exists to surface.

**Cargo-resolved.** Counts here honor `package = "..."` renames, `workspace = true` inheritance, and `[target.<cfg>.dependencies]`. Stdlib (`std`, `core`, `alloc`) and intra-crate sub-module references are excluded.

## Crates in this repo

| package | module | inbound use sites | distinct caller crates | distinct caller repos | codex sites |
|---|---|---:|---:|---:|---:|
| `pulldown-cmark` | `pulldown_cmark` | 8 | 1 | 1 | 8 |

## `pulldown-cmark`

### Inbound symbols (8 distinct, 8 total `use` sites)

Sorted by codex demand, then total demand. The first symbols are the priority queue for porting `pulldown-cmark`.

#### `pulldown_cmark::CodeBlockKind` — 1 site(s) (1 codex, 0 other, 1 caller crates)

- ✅ `codex-kotlin/codex-tui` `src/markdown_render.rs:14` — `use pulldown_cmark::CodeBlockKind`

#### `pulldown_cmark::CowStr` — 1 site(s) (1 codex, 0 other, 1 caller crates)

- ✅ `codex-kotlin/codex-tui` `src/markdown_render.rs:15` — `use pulldown_cmark::CowStr`

#### `pulldown_cmark::Event` — 1 site(s) (1 codex, 0 other, 1 caller crates)

- ✅ `codex-kotlin/codex-tui` `src/markdown_render.rs:16` — `use pulldown_cmark::Event`

#### `pulldown_cmark::HeadingLevel` — 1 site(s) (1 codex, 0 other, 1 caller crates)

- ✅ `codex-kotlin/codex-tui` `src/markdown_render.rs:17` — `use pulldown_cmark::HeadingLevel`

#### `pulldown_cmark::Options` — 1 site(s) (1 codex, 0 other, 1 caller crates)

- ✅ `codex-kotlin/codex-tui` `src/markdown_render.rs:18` — `use pulldown_cmark::Options`

#### `pulldown_cmark::Parser` — 1 site(s) (1 codex, 0 other, 1 caller crates)

- ✅ `codex-kotlin/codex-tui` `src/markdown_render.rs:19` — `use pulldown_cmark::Parser`

#### `pulldown_cmark::Tag` — 1 site(s) (1 codex, 0 other, 1 caller crates)

- ✅ `codex-kotlin/codex-tui` `src/markdown_render.rs:20` — `use pulldown_cmark::Tag`

#### `pulldown_cmark::TagEnd` — 1 site(s) (1 codex, 0 other, 1 caller crates)

- ✅ `codex-kotlin/codex-tui` `src/markdown_render.rs:21` — `use pulldown_cmark::TagEnd`
