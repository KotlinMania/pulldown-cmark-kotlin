import Testing
import PulldownCmark

@Suite("PulldownCmark Export Smoke Tests")
struct PulldownCmarkExportTests {
    @Test("Swift module loads cleanly")
    func testSwiftModuleLoads() throws {
        #expect(true)
    }
}
